// @flow strict
/*::
import type { Component, ElementNode } from "@lukekaalim/act";
*/

import { h } from "@lukekaalim/act"

/*::
export type ActionTool = {
  type: 'action',
  iconURL?: string,
  title?: string,

  disabled?: boolean,
  onAction?: () => mixed,
}
export type SwatchTool = {
  type: 'swatch',
  iconURL?: string,
  title?: string,

  disabled?: boolean,
  tools: Tool[],
}
export type Tool =
  | ActionTool
  | SwatchTool
*/

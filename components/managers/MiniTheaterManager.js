// @flow strict

import { h, useState } from "@lukekaalim/act";

/*::
type MiniTheaterManagerSelection =
  | { type: 'none' }
  | { }
*/

const useMiniTheaterManagerSelection = () => {
}

export const MiniTheaterManager = () => {
  const [view, setView] = useState({ type: 'new-character-piece' })
}

const MiniTheaterPiecesManager = ({  }) => {
  return h('ul', {}, [

  ])
}
// @flow strict

import { c } from "@lukekaalim/cast";

/*::
import type { Cast } from "@lukekaalim/cast/main";
import type { BoardPosition } from "../../encounter/map";
import { castBoardPosition } from "../../encounter/map";

export type MiniTheaterShape =
  | { type: 'box' }
  | { type: 'sphere' }
  | { type: 'cylinder' }
  | { type: 'cone' }
  | { type: 'line' }
*/